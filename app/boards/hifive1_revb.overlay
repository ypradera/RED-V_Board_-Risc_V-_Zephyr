/*
 * Overlay for SparkFun RED-V RedBoard
 * Disables SPI1/SPI2 to free GPIO 5 for LED.
 */
/ {
	leds {
		compatible = "gpio-leds";
		led0: led_0 {
			gpios = <&gpio0 5 GPIO_ACTIVE_HIGH>;
			label = "RED-V Built-in LED";
		};
	};
};

&spi1 { status = "disabled"; };
&spi2 { status = "disabled"; };
